Featured Need to be added :

- if provided the pdf then able to extract images from it
- get image context from it
- and then just add normal typical rag features with correlation using no4j
- only store the data into new database or table which is specific to that user, checkout the feature something like that exists or not
